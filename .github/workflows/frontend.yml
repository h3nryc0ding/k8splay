name: Frontend CI

on:
  workflow_call:
  workflow_dispatch:

env:
  NODE_VERSION: 18
  PNPM_VERSION: 8
  WORKING_DIRECTORY: frontend

defaults:
  run:
    # TODO: reuse environment variable
    working-directory: frontend

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm-node
      - name: Run prettier and eslint
        run: pnpm lint

  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm-node
      - name: Check Frontend
        run: pnpm check

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm-node
      - name: Build Frontend
        run: pnpm build

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-pnpm-node
      - uses: ./.github/actions/setup-playwright
      - name: Test Frontend
        run: pnpm test
